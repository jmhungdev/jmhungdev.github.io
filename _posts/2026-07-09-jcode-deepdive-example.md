---
layout: post
title: "jcode Deepdive: Example (delete me)"
---
<!-- PLACEHOLDER POST: this file demonstrates the post layout for the Deepdive series — headings, links, and code blocks. Delete it, or copy it into a new file in _posts/ named YYYY-MM-DD-your-title.md to write a real one. -->

This is a sample paragraph with an [inline link](https://jekyllrb.com/docs/posts/) to show how links render inside a post.

## A subheading

Posts are plain Markdown files in `_posts/`, named `YYYY-MM-DD-title.md`. The date in the filename controls sort order and the date shown here, and the URL lives under `/deepdive/`.

```js
function greet(name) {
  return `hello, ${name}`;
}
```

Regular paragraphs continue normally after a code block.
